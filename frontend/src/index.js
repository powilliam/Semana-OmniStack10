import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// What React is: 
// Component -> class/function that returns HTML tags
// Props -> attributes in the Component
// State -> information that will be manipulated by the component
